# Batty — DevOps

## Role
Aspire AppHost resource wiring, CI pipeline (fallout build system), GitHub Actions for `document`.

## Project Context
AppHost.cs wires postgres, minio (CommunityToolkit.Aspire.Hosting.Minio), rabbitmq, keycloak, api, migrations. CI uses fallout build (build.sh). Requested by Cyrille NDOUMBE.
